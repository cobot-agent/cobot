{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the skill"
    },
    "format": {
      "type": "string",
      "enum": ["yaml", "markdown"],
      "description": "Format of the skill file"
    },
    "content": {
      "type": "string",
      "description": "Content of the skill"
    }
  },
  "required": ["name", "format", "content"]
}
